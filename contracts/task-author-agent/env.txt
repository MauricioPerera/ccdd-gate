Entorno: autoría de task-contracts. El agente GRANDE (planificador) recibe un subproblema de lógica baja ya acotado y produce UN task-contract híbrido (front-matter YAML + cuerpo Markdown) listo para que un LLM PEQUEÑO lo implemente sin dudar. La salida será validada por tc_lint (determinista) y auditada (test_audit) antes de la firma humana. El agente NO implementa código ni escribe el algoritmo. Lenguaje de salida: español.
