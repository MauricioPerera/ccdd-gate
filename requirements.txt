# --- Núcleo del gate (gate, tc_lint, task_gate, métricas, orchestrator, MCP) ---
# Solo necesita pyyaml. Es lo único que hace falta para el producto determinista.
pyyaml>=6.0

# --- Motor CCDD (ccdd.py: lint/assemble/export + gate L2 de gobernanza) ---
jsonschema>=4.0      # validación de contratos contra el schema
cryptography>=42     # firmas Ed25519 de revisores (keygen/attest/quórum en diff L2)

# --- Opcional ---
# anthropic : solo para el backend `anthropic` de los runners advisory.
#             Los backends ollama / openai-compatible (LM Studio, vLLM) usan urllib (stdlib).
# anthropic>=0.40
#
# tree-sitter : backend UNIVERSAL de métricas + firma para lenguajes no-Python (decisión #7).
#               Dep OPCIONAL por lenguaje: sin la gramática instalada, Python (backend nativo)
#               sigue funcionando igual, y measure_complexity/lint_task_contract degradan a
#               vacío/aridad genérica para ESE lenguaje sin lanzar. Soporta: typescript/tsx/
#               javascript, rust, go, java, csharp, php, ruby, kotlin, c, swift, cpp.
#               Instalar vía extra del paquete (recomendado):
#                 pip install ccdd-gate[rust]      # un lenguaje
#                 pip install ccdd-gate[all]       # los 11
#               O manualmente (ejemplo Rust):       pip install tree_sitter tree_sitter_rust
# tree_sitter>=0.23
# tree_sitter_rust>=0.23
